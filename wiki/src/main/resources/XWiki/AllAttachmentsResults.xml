<?xml version="1.0" encoding="UTF-8"?>

<xwikidoc>
<web>XWiki</web>
<name>AllAttachmentsResults</name>
<language></language>
<defaultLanguage>en</defaultLanguage>
<translation>0</translation>
<parent>XWiki.AllAttachments</parent>
<creator>XWiki.Admin</creator>
<author>XWiki.Admin</author>
<customClass></customClass>
<contentAuthor>XWiki.Admin</contentAuthor>
<creationDate>1252454400000</creationDate>
<date>1252454400000</date>
<contentUpdateDate>1252454400000</contentUpdateDate>
<version>1.1</version>
<title>$msg.get("xe.index.attachments.results")</title>
<template></template>
<defaultTemplate></defaultTemplate>
<validationScript></validationScript>
<comment></comment>
<minorEdit>false</minorEdit>
<syntaxId>xwiki/2.0</syntaxId>
<hidden>false</hidden>
<content>{{velocity}}
## ============================================================================================
## This page is called from live grids via Ajax with the argument xpage=plain. It returns a
## set of results serialized in JSON.
## @programming This service page needs to be saved by a user with the programming access level
## to work as expected
## ============================================================================================
#if ($request.get("xpage") &amp;&amp; $request.get("xpage") == "plain")
#set( $offset = $util.parseInt( $request.get( "offset" ) ) )
## offset starts from 0 in velocity and 1 in javascript
#set( $off = $offset - 1 )
#set( $limit = $util.parseInt( $request.get( "limit" ) ) )
#set($title = $request.get("doc.name"))
#set($author = $request.get("doc.author"))
#set($space = $request.get("doc.space"))
#set($filename = $request.filename)
#set($formatDate = "yyyy MMMM dd, HH:mm")
#if($title)
  #set($title = $title.trim().toLowerCase())
  #set($sql = "and lower(doc.fullName) like '%${title}%' ")
#else #set($sql = "and lower(doc.fullName) &lt;&gt; '' ") #end
#if($author)
  #set($author = $author.trim().toLowerCase())
  #set($sql = $sql + "and lower(attach.author) like '%${author}%' ")
#end
#if($filename)
  #set($filename = $filename.trim().toLowerCase())
  #set($sql = $sql + "and lower(attach.filename) like '%${filename}%' ")
#end
#if($space)
  #set($space = $space.trim().toLowerCase())
  #set($sql = $sql + "and lower(doc.space) like '%${space}%' ")
#end
## 
## Exclude Blacklisted spaces ($blacklistedSpaces is set in xwikivars.vm)
##
#foreach ($blacklistedSpace in $blacklistedSpaces)
  #set ($sql = "${sql} and doc.space &lt;&gt; '$blacklistedSpace'")
#end
#if("$!request.sort" != "") #set($order = $request.sort) #else #set($order="attach.filename") #end
#if($order == "filename") #set($order = "attach.filename") #end
#if("$!request.dir" != "") #set($dir = $request.dir) #else #set($dir = "asc") #end
#set($sql = "select doc.fullName, attach from XWikiDocument as doc, XWikiAttachment as attach where attach.docId=doc.id " + $sql + " order by $order $dir")
#set($items = $xwiki.search($sql, $limit, $off))
#set($totalItems = $xwiki.search($sql).size())
#set($returnedItems = $items.size())
## ==============================================
## json starts
## ==============================================
{
"totalrows": $totalItems,
"returnedrows": #if($returnedItems &lt; $limit) $returnedItems #else $limit #end,
"offset": $offset,
"reqNo": $request.reqNo,
"rows": [
#foreach($item in $items)
  #foreach($element in $item)
    #if($velocityCount==1) 
      #set($docName=$element) 
      #set($acclev = $xwiki.hasAccessLevel("view", $context.user, $docName)) 
    #else 
      #set($attachment=$element) 
    #end
  #end
  #set($document = $xwiki.getDocument($docName))  
  #if($velocityCount &gt; 1) , #end
  {"acclev"    : $acclev,     
     "page"  : "$document.name",
     "space" : "$document.space",
     "fullname"  : "$document.fullName"#if ($acclev),
     "filename"    : "$attachment.filename",
     "fileurl" : "$document.getAttachmentURL($attachment.filename)",
     "url" : "$document.getURL('view')",
     "spaceurl" : '$xwiki.getURL("${document.space}.WebHome","view")',
     "date" : "$xwiki.formatDate($attachment.date,$formatDate)",
     "author" : "$attachment.author",
     "authorname" : "$xwiki.getDocument($attachment.author).name",
     "authorurl" : "$xwiki.getURL($attachment.author,'view')",
     "type" : "$attachment.getMimeType($context.context)"
   #end ## acclev
   }  
#end
]}
## ==============================================
## json ended
## ==============================================
#end
{{/velocity}}</content>
</xwikidoc>